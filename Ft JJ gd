Do not implement any of A/B/C/D/F yet.

Based on our testing, we have confirmed:

1. The dashboard renders correctly in the iframe.
2. The layout becomes corrupted only when domToPng() converts the DOM to PNG.
3. Therefore the issue is with the DOM-to-image rendering path, not the dashboard's actual CSS layout.

From the options you identified, I want you to investigate Approach E (Playwright/Puppeteer) first, but DO NOT implement it yet.

Our QA Dashboard is accessed through a server URL.

Please investigate the feasibility of:

Server URL
→ Playwright/Chromium
→ render the actual QA Releases Dashboard
→ wait for the page/data/fonts/charts to finish rendering
→ take a native browser screenshot
→ pass the PNG into our EXISTING email-report/.eml flow

Before making code changes, answer these questions:

1. Where is the current screenshot generated — frontend or backend?

2. What exact server URL is used to access the QA Dashboard?

3. How does the QA Dashboard authenticate?
   - SSO?
   - cookies?
   - JWT?
   - localStorage/sessionStorage?
   - other?

4. Can Playwright access the same authenticated dashboard without storing or exposing user credentials?

5. Can Playwright reuse the existing authenticated session safely if required?

6. Does our backend already have Playwright/Puppeteer/Chromium available?

7. What would be the impact of running Chromium on the server?
   - memory
   - CPU
   - concurrency
   - deployment requirements

8. Most importantly, create a SMALL proof-of-concept only if feasible:
   
   Server URL
   → Playwright Chromium
   → authenticated Releases Dashboard
   → wait for rendering
   → native screenshot

   Do not modify the production email flow yet.

9. Compare the Playwright screenshot against the current domToPng screenshot.

I want to see whether Playwright produces the correct screenshot while domToPng produces the overlapping screenshot.

DO NOT change:
- dashboard CSS
- iframe CSS
- browser zoom
- card dimensions
- existing .eml generation
- MIME/Content-ID implementation

The purpose of this step is only to establish whether native Chromium screenshot is a reliable replacement for domToPng in our architecture.

After the investigation, report:

A. Is Playwright feasible?
B. Can it access our server URL?
C. Can authentication be handled securely?
D. Does the Playwright screenshot eliminate the overlapping?
E. What changes would be required to move screenshot generation to Playwright?
F. Any performance/deployment concerns?

Do not implement the production replacement until I review these findings.
