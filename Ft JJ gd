We have now isolated the screenshot problem.

The important finding is:

The Release Dashboard is rendered correctly inside the email-report iframe BEFORE screenshot conversion.

I inspected the iframe visually and there is NO overlapping there.

The problem appears only at this step:

Correct iframe DOM
        ↓
      domToPng()
        ↓
    Generated PNG
        ↓
❌ Text/layout overlapping

For example, in the generated PNG:
- RELEASE HEALTH → 94% overlaps the heading
- RISKS / IMPEDIMENTS → "Inconsistent" overlaps the heading
- RELEASE PIPELINE → Progress/legend elements overlap the surrounding content

But these same elements look correctly positioned in the iframe immediately before domToPng() runs.

Therefore, we have confirmed that the dashboard's normal CSS/layout is not the primary problem.

Do NOT change:
- dashboard CSS
- card heights
- iframe width
- browser zoom
- responsive breakpoints
- margins/padding
- overflow
- release dashboard component layout

Those are not the root cause we are investigating now.

The problem is specifically the DOM-to-PNG conversion performed by domToPng.

I want you to now investigate why domToPng is changing the visual layout during conversion.

Before making any fix, tell me:

1. Which domToPng package/library are we using?
2. What version?
3. What exact domToPng() call/options are being used?
4. Does the library clone/reconstruct the DOM internally?
5. How does it handle:
   - fonts
   - computed CSS
   - flex/grid
   - transforms
   - CSS variables
   - SVG
   - pseudo-elements
   - devicePixelRatio
   - scaling
   - percentage dimensions
6. Why could an element that is correctly positioned in the iframe become overlapping only after domToPng conversion?

Do not implement a fix yet.

First give me the technical root-cause analysis of the domToPng conversion issue.

After we understand that, we will decide whether to:
A. configure/fix domToPng, or
B. replace domToPng with a native browser screenshot approach such as Playwright.
