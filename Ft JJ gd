Before making ANY further changes, I want you to perform one diagnostic test to identify exactly where the overlap is being introduced.

When I click "Email Report", your current flow is:

User clicks Email Report
→ hidden iframe is created
→ dashboard is cloned into iframe
→ styles are copied
→ fonts/layout stabilize
→ domToPng(clone) is called
→ PNG is generated
→ PNG is added to .eml

I need to determine whether the layout is already broken BEFORE domToPng(), or whether domToPng() is causing the overlap.

DO NOT change the CSS, layout, iframe width, zoom handling, screenshot library, or .eml generation yet.

Temporarily modify ONLY the debugging flow.

1. When the email report iframe is created and the dashboard clone has finished rendering, make the iframe temporarily visible.

2. Keep it at exactly the same dimensions/styles currently used for the email screenshot.

3. Pause BEFORE calling domToPng().

4. I want to visually inspect the actual rendered iframe.

5. Specifically check these three areas:
   - RELEASE HEALTH → "94%" overlapping the heading
   - RISKS / IMPEDIMENTS → "Inconsistent" overlapping the heading
   - RELEASE PIPELINE → "Progress" / progress bar / legend overlapping

6. Also log getBoundingClientRect() for the relevant elements so we can see their actual:
   - top
   - left
   - width
   - height
   - bottom
   - right

7. Then take two screenshots for comparison:
   A. The iframe BEFORE domToPng()
   B. The final PNG generated by domToPng()

Do not make any permanent fixes yet.

The only conclusion I want from this test is:

CASE A:
The iframe itself already has the overlapping layout before domToPng().
→ Then the problem is in the cloned DOM/CSS/layout.

OR

CASE B:
The iframe looks perfectly correct before domToPng(), but the generated PNG has overlapping text.
→ Then the problem is specifically in domToPng()/its rendering process.

Please give me:
- Screenshot A: iframe before domToPng
- Screenshot B: final PNG
- Bounding-rectangle logs
- Your conclusion: CASE A or CASE B
- The exact element(s) that are overlapping

Do not try another fix until this diagnostic is completed.
